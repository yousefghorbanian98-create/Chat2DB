# Proposal — not shipped yet

Built and captured from the running app so it can be judged before it is released.
Nothing here is in a version the user can install: `package.json` still says 0.4.0.

| File | What changed |
|---|---|
| `A-home.png` | No menu bar, no tab strip. The wordmark is centred at the top of the launcher; the three window actions sit on the right. Recent projects now include the unfinished (autosaved) one. |
| `B-home-delete.png` / `B-delete-crop.png` | Hovering a project card reveals a delete button. |
| `C-editor.png` | The editor: the wordmark has flown to the top-left corner and is the way home; the save bar above the preview is gone. |
| `D-toast.png` | A notification that can actually be read — dark card, coloured icon. |

Still to confirm with the user: whether the launcher should keep the three icon
buttons at the top right, or whether those should move into the page as well.
