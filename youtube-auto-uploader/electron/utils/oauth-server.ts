import http from 'node:http';
export async function oauthServer():Promise<{redirectUri:string;code:Promise<string>;close:()=>void}>{let resolveCode:(code:string)=>void=()=>undefined;let rejectCode:(error:Error)=>void=()=>undefined;const code=new Promise<string>((resolve,reject)=>{resolveCode=resolve;rejectCode=reject});const server=http.createServer((request,response)=>{const url=new URL(request.url??'/',`http://${request.headers.host??'127.0.0.1'}`);if(url.pathname!=='/callback'){response.writeHead(404).end();return}const error=url.searchParams.get('error');const value=url.searchParams.get('code');if(error||!value){rejectCode(new Error(error??'OAuth code missing'));response.writeHead(400,{'Content-Type':'text/plain'}).end('Google sign-in failed. You may close this window.');return}resolveCode(value);response.writeHead(200,{'Content-Type':'text/html'}).end('<h2>Connected successfully.</h2><p>You can close this window and return to the app.</p><script>setTimeout(()=>window.close(),1500)</script>')});await new Promise<void>((resolve,reject)=>{server.once('error',reject);server.listen(0,'127.0.0.1',()=>resolve())});const address=server.address();if(!address||typeof address==='string')throw new Error('Unable to start OAuth callback');return{redirectUri:`http://127.0.0.1:${String(address.port)}/callback`,code,close:()=>server.close()}}
