import {app,BrowserWindow,Menu,Tray,nativeImage} from 'electron';import path from 'node:path';
export function createTray(window:BrowserWindow):Tray{const icon=app.isPackaged?path.join(process.resourcesPath,'icon.png'):path.join(process.cwd(),'resources','icon.png');const tray=new Tray(nativeImage.createFromPath(icon).resize({width:20,height:20}));tray.setToolTip('YouTube Auto-Uploader');tray.setContextMenu(Menu.buildFromTemplate([{label:'Open',click:()=>{window.show();window.focus()}},{type:'separator'},{label:'Quit',click:()=>{app.quit()}}]));tray.on('double-click',()=>window.show());return tray}
