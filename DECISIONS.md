# تصمیم‌های ثبت‌شده — Forge

> هر انحراف از طرحِ اولیه باید اینجا با دلیل ثبت شود (گیتِ «خروج از محدوده»).

## D-1 · حذف DaisyUI از فرانت‌اند

**طرح اولیه:** Tailwind 4 + DaisyUI 5 + Motion (ردیف M-5 ترکر)
**انجام‌شده:** Tailwind 4 + توکن‌های DESIGN.md + Motion

**دلیل:** DaisyUI تمِ خودش را با متغیرهای `base-100`, `primary`, … تعریف می‌کند.
نگاشتِ این‌ها به توکن‌های DESIGN.md دو منبعِ حقیقت برای رنگ می‌ساخت و گیتِ
طراحی (FE-4-G1: «هر رنگ باید از توکن بیاید») را عملاً غیرقابلِ اجرا می‌کرد —
کلاس‌های آماده‌ی DaisyUI رنگ را از تمِ خودش می‌خوانند، نه از DESIGN.md.

**جایگزین:** توکن‌ها با `@theme` در Tailwind 4 تعریف شده‌اند و از متغیرهای
`--fg-*` می‌خوانند، بنابراین تغییرِ تم فقط با عوض شدنِ یک صفتِ `data-theme`
انجام می‌شود و هیچ کلاسی رنگِ ثابت ندارد (AC-7).

**هزینه:** چند قطعه (دکمه، کارت، قرص) دستی نوشته شد. تعدادشان کم بود (~۸ قطعه).

## D-2 · JSON به‌جای SQLite برای انبار

**طرح اولیه:** SQLite محلی (ردیف M-7)
**انجام‌شده:** فایل JSON پشتِ رابطِ `Store`

**دلیل:** هیچ وابستگیِ بومی (native) معرفی نشود تا بیلد روی runner ویندوزی
قطعی باشد. `better-sqlite3` نیاز به کامپایل دارد و `node:sqlite` در Node ۲۲
هنوز آزمایشی است و نیاز به فلگ دارد.

**مسیر برگشت:** متدهای `Store` همان امضای یک پایگاه را دارند؛ جایگزینی با
SQLite بدون تغییر در لایه‌های بالاتر ممکن است.

## D-3 · node.exe رسمی به‌جای nexe/pkg برای sidecar

**طرح اولیه:** تبدیلِ سرور Node به exe با `nexe`
**انجام‌شده:** دانلودِ `node.exe` رسمی در CI و اجرای `server/dist/index.js` کنار آن

**دلیل:** `nexe` و `pkg` روی Node ۲۲ غیرقابل‌اتکا هستند و برای نسخه‌های جدید
پیش‌ساخته ندارند. دانلودِ باینری رسمی قطعی است.

## D-4 · باندل با esbuild به‌جای tsc

**دلیل:** ماژول‌های ESM در Node به پسوندِ فایل در import نیاز دارند؛
`tsc` مسیرهای بدون پسوند را بازنویسی نمی‌کند. esbuild کل سرور را در یک فایل
باندل می‌کند که برای بسته‌بندیِ sidecar هم بهتر است.
تایپ‌چک همچنان با `tsc --noEmit` انجام می‌شود (گیت G2).

## D-5 · منبعِ DESIGN.md

**انجام‌شده:** https://getdesign.md/cursor/design-md (تحلیلِ سیستم طراحی Cursor)
**نکته:** ریپوی `VoltAgent/awesome-claude-design` فقط README دارد؛
۶۸ فایل DESIGN.md روی سایتِ خارجی میزبانی می‌شوند، نه در ریپو
(این با بررسیِ مستقیمِ درختِ ریپو کشف شد — نه از روی ادعا).
مواردی که برای دسکتاپ بازتعریف شده‌اند در DESIGN.md با **[برگرفته]** مشخص شده‌اند.

## D-6 · پوششِ `scripts/tauri.mjs` دورِ دستورِ tauri

**طرح اولیه:** فراخوانیِ مستقیمِ `@tauri-apps/cli` از پایپ‌لاین
**انجام‌شده:** اسکریپتِ `tauri` در `forge/package.json` یک پوششِ نازک است که
دستور را اجرا می‌کند و بعد از موفقیت، خروجیِ بسته‌بندی را به
`src-tauri/target/release/bundle/` می‌آورد.

**دلیل:** با `‎--target x86_64-pc-windows-msvc`، cargo خروجی را زیر
`target/<triple>/release/` می‌گذارد، در حالی که پایپ‌لاین از
`target/release/bundle/nsis/*.exe` برمی‌دارد — مرحله‌ی آپلود با
`if-no-files-found: error` شکست می‌خورد. چون امکانِ ویرایشِ
`.github/workflows/**` از این محیط وجود ندارد، تطبیقِ مسیر به عهده‌ی
اسکریپتِ ساخت گذاشته شد تا نیاز به ویرایشِ دستی نباشد.

**نکته:** اسکریپت هیچ چیزی را جعل نمی‌کند — فقط فایل‌هایی که tauri واقعاً
ساخته را از هر کجای `target/` که باشند پیدا کرده و کپی می‌کند. اگر چیزی
پیدا نکند، درختِ `target/` را چاپ می‌کند.

**مسیر برگشت:** اگر روزی workflow اصلاح شود (`target/<triple>/release/...` یا
`if-no-files-found: warn`)، حذفِ این پوشش و بازگرداندنِ
`"tauri": "tauri"` کافی است.

## D-7 · آیکون‌ها باید ۸-بیت باشند

**دلیل:** پیش‌فرضِ ImageMagick برای PNG عمقِ ۱۶ است و
`tauri::generate_context!()` با خطای
`Unsupported PNG bit depth: Sixteen` از کار می‌افتد. همه‌ی آیکون‌ها
(و تک‌تکِ فریم‌های ICO) با `PNG32` و `‎-depth 8` بازتولید شدند.

## D-8 · بازنگریِ رابط — سیستمِ طراحی روی کاغذ بود، نه در کد

**اتفاق:** DESIGN.md یک مقیاسِ تایپوگرافیِ کامل (§۳) و قواعدِ قطعات (§۴) دارد،
ام تا نسخه‌ی ۰٫۱٫۰ هیچ‌کدام پیاده‌سازی نشده بودند: کامپوننت‌ها اندازه‌ی دلخواه
داشتند (`text-[22px]`, `text-[12px]`), هیچ آیکونی در رابط نبود، صفحه‌ی آغاز یک
پاراگرافِ تنها بود، و وضعیتِ «غایب/ناقصِ» ابزارها فقط به شکلِ چند قرصِ کوچک
نمایش داده می‌شد بی آنکه بگوید یعنی چه و چه باید کرد.

**انجام‌شده:** مقیاسِ §۳ به utility تبدیل شد، مجموعه‌ی آیکونِ داخلی (بدون
وابستگی) اضافه شد، صفحه‌ی آغاز با چهار شروع‌کننده بازنوشته شد، و یک «راهنمای
وصل‌کردنِ ابزارها» ساخته شد که برای هر ابزار نقش، نیازمندی و نشانیِ منبع را
می‌گوید. نشانگرِ فوکوس و احترام به `prefers-reduced-motion` هم اضافه شد
(سند در این باره ساکت بود).

**دلیلِ ثبت:** این یک انحراف از مسیرِ اولیه است — خروجیِ بصریِ نسخه‌ی ۰٫۱٫۰
پایین‌تر از انتظار بود و علتش «نداشتنِ منبعِ طراحی» نبود، بلکه **اعمال‌نکردنِ
منبعی بود که از پیش داشتیم**.

**یادداشت:** منابعِ طراحیِ اوپن‌سورسِ فراوانی در دسترس بود، اما عملاً فقط یکی
(تحلیلِ سیستمِ طراحیِ Cursor) مصرف شد. این انتخاب در دورِ بعد باید بازنگری شود.

## D-9 · به‌روزرسانیِ تفاضلی به‌جای نصب‌کننده‌ی کامل

**مسئله:** نصب‌کننده ۲۱٫۷ مگابایت است و تقریباً هیچ‌وقت لازم نیست دوباره
دانلود شود — چون پوسته‌ی Tauri و node.exe ثابت‌اند و چیزی که عوض می‌شود
دارایی‌های رابط و باندلِ سرور است.

**انجام‌شده:** مانیفستِ sha256 برای هر فایلِ `server/dist` + بسته‌ی tar.gz که
**فقط** فایل‌های تغییرکرده را دارد. کلاینت (سرورِ داخلی) مانیفستِ منتشرشده را
از دارایی‌های همان Release می‌گیرد، تفاضل را حساب می‌کند، بسته را می‌گیرد،
درهم‌سازیِ تک‌تکِ فایل‌ها را **پیش از نوشتن** تأیید می‌کند و بعد با نسخه‌ی
پشتیبان جایگزین می‌کند. اندازه‌گیری‌ی واقعی: یک تغییرِ دو فایلی = ۴٫۴ کیلوبایت
در برابرِ ۲۲۵ کیلوبایتِ بسته‌ی کامل (۲٪).

**چرا tar.gz و نه zip:** استخراجِ zip بدون وابستگی دردسر دارد؛ tar با
`zlib.gunzipSync` و یک خواننده‌ی حداقلیِ ustar در حدود ۶۰ خط حل شد و هیچ
بسته‌ی تازه‌ای به پروژه اضافه نکرد (همان رویه‌ی مجموعه‌ی آیکون در D-8).

**محدودیتِ صادقانه:** اگر `index.js` یا `package.json` عوض شود، برنامه باید
دوباره اجرا شود (رابط پیام می‌دهد و خودکار ری‌استارت نمی‌کند). بیشترِ
به‌روزرسانی‌ها فقط دارایی‌های رابط هستند و با یک بارگذاریِ دوباره اعمال می‌شوند.

## D-10 · تغییرِ جهت به «کارگاه» و حسابرسیِ پیش از آن

**درخواست:** «اول کل فایل‌ها را بگرد ببین چیزی گم نشده باشد… مایلم کل برنامه
تماماً تکمیل شده باشد» + انتخابِ جهتِ «ج · کارگاه».

**حسابرسی → ۵ کاستیِ واقعی پیدا و برطرف شد:**
1. `forge/tsconfig.tsbuildinfo` در گیت بود (خروجیِ ساخت) — حذف و نادیده گرفته شد.
2. پنجره اندازه نداشت → پیش‌فرضِ ۸۰۰×۶۰۰ و پنلِ کناری له می‌شد؛ الان
   ۱۴۴۰×۹۰۰ با کمینه‌ی ۱۰۴۰×۶۸۰ (جمعِ سه ستون).
3. تم ذخیره نمی‌شد و هر بار بازگشایی به پیش‌فرض برمی‌گشت — الان ماندگار است.
4. هیچ ErrorBoundary‌ای نبود: یک خطا کلِ پنجره را سفید می‌کرد.
5. حذفِ نشست وجود نداشت (فقط ساخت و انتخاب) — مسیر و رابط و ۲ تست اضافه شد.

**تغییرِ جهت:** DESIGN.md به «نسخه‌ی ۲ · کارگاه» بازنویسی شد (§۱ حال‌وهوا،
§۲ پالتِ گرمِ روشن/تاریک، §۴ شعاع ۱۶/۱۰/۸ و «بالا آمدنِ» بی‌سایه، §۱۰ جدولِ
حرکت با easingِ فنری). قانونِ «بدون سایه» از نسخه‌ی ۱ حفظ شد — «بالا آمدن»
با جابه‌جاییِ ۲px + حاشیه + سطح ساخته می‌شود.

**مواردی که عمداً باز مانده و به تصمیم نیاز دارد:**
- FE-5-1 اسکرین‌شات: در این محیط غیرممکن (دانلودِ مرورگر مجاز نیست).
- INT-7 ارتقای `actions/*`: فایلِ workflow در دستِ کاربر است.
- انتشارِ بسته‌ی تفاضلی در CI: نیاز به یک مرحله در workflow (دستِ کاربر).
