node_modules/
dist/
dist-ssr/
.data/
src-tauri/target/
src-tauri/gen/

# CI با npm ci اجرا می‌شود و به lockfile نیاز دارد،
# بنابراین علی‌رغم قانونِ ریشه، آن را در این پوشه نگه می‌داریم
!package-lock.json

# خروجیِ TypeScript (نباید در گیت باشد)
tsconfig.tsbuildinfo

# خروجی‌های انتشار
dist-update/
.update-base/
