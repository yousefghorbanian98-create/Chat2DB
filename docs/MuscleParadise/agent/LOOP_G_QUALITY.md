# LOOP G — کیفیت و اندازه‌گیری
## ۱۶ تسک · ~۲ هفته · وابسته به B، C، D

**هدف:** تبدیل هر «نامعلوم» به یک عدد.

> امروز LCP، CLS، INP و P95 همه «نامعلوم» هستند. GitHub Actions مرورگر دارد —
> چیزهایی که در سندباکس محلی «مسدود» بودند، در CI کاملاً شدنی‌اند. **بهانه‌ای نیست.**

---

# فاز G1 — تست E2E (تسک ۰۱–۰۶)

## G-01 · راه‌اندازی Playwright
`npm i -D @playwright/test` · پیکربندی برای دو پوسته · فیکسچر: هسته + DB تازه.
**verify:** `npx playwright test --list` → ۰ خطا

## G-02 · E2E: احراز هویت و ایزولاسیون
| **فایل‌ها** | `e2e/auth.spec.ts` |
|---|---|
```
۱. ورود مالک → داشبورد
۲. ورود ورزشکار → مشاهدهٔ خود
۳. 🔴 ورزشکار نمی‌تواند فهرست اعضا را ببیند
```
مسیر ۳ مهم‌ترین تست کل پروژه است — C4 را اثبات می‌کند.
**verify:** `npx playwright test e2e/auth.spec.ts` → ۳/۳ سبز

---

## G-03 · E2E: سنجش و برنامه (ایمنی)
| **فایل‌ها** | `e2e/assessment.spec.ts`، `e2e/program.spec.ts` |
|---|---|
```
۴. ثبت JP7 → دانلود PDF فارسی سالم (متن فارسی قابل استخراج باشد)
۵. ساخت برنامه → dry-run → apply → تلاش دوم = ۴۰۹
۶. 🔴 مصدومیت زانو → حرکت مغایر در برنامه نیست
```
مسیر ۶ قلب ایمنی محصول است (C5).
**verify:** `npx playwright test e2e/assessment.spec.ts e2e/program.spec.ts`

---

## G-04 · E2E: عملیات روزانه
| **فایل‌ها** | `e2e/operations.spec.ts` |
|---|---|
```
۷. اسکن QR → ثبت حضور
۸. ثبت پرداخت → رسید PDF
۹. ثبت ست تمرین توسط ورزشکار
```
**verify:** `npx playwright test e2e/operations.spec.ts`

---

## G-05 · E2E: دوام و آفلاین
| **فایل‌ها** | `e2e/resilience.spec.ts` |
|---|---|
```
۱۰. پشتیبان → بازیابی → اعداد یکسان
۱۱. 🔴 آفلاین → ثبت ست → آنلاین → همگام‌سازی
۱۲. حرکت کاهش‌یافته → بدون انیمیشن
```
مسیر ۱۱ ادعای local-first را اثبات می‌کند (با `context.setOffline(true)`).
**verify:** `npx playwright test e2e/resilience.spec.ts`

## G-06 · E2E در CI
job جدید در `mp-ci.yml`. اگر کند بود، فقط روی PR به main.

---

# فاز G2 — عملکرد (تسک ۰۷–۱۱)

## G-07 · Lighthouse CI
**هدف:** perf ≥ ۹۰ · a11y ≥ ۹۵ · best-practices ≥ ۹۰
روی هر دو پوسته. job شبانه (کند است، مسدودکنندهٔ PR نباشد).

## G-08 · axe روی همهٔ مسیرها
`@axe-core/cli` — **۰ نقض critical**.

## G-09 · تست بار با k6
```
P95 خواندنی  < ۱۲۰ ms
P95 نوشتنی   < ۳۰۰ ms
۵۰ req/s پایدار
```
روی سخت‌افزار هدف (Core i3، ۸GB). اگر CI قوی‌تر است، عدد را نرمال کنید.

## G-10 · پروفایل کوئری DB
هر کوئری > ۵۰ ms → `EXPLAIN QUERY PLAN` → ایندکس گمشده را پیدا کنید.

## G-11 · اجرای بودجهٔ باندل در CI
`scripts/check-bundle-budget.mjs` (C-03) به‌عنوان دروازهٔ مسدودکننده.

---

# فاز G3 — کیفیت تست (تسک ۱۲–۱۶)

## G-12 · تست جهش روی `core/`
`mutmut` — **هدف: امتیاز ≥ ۷۰٪**.
> پوشش ۹۰٪ می‌تواند دروغ بگوید. اگر تغییر `>` به `>=` هیچ تستی را نشکند،
> تست‌ها فقط کد را «لمس» می‌کنند، نه «بررسی».

## G-13 · ماتریس مجوز: ۴۹ مسیر × ۶ نقش
تست پارامتری با جدول کد وضعیت مورد انتظار.
**تنها راه اطمینان از نشت‌نکردن دسترسی هنگام افزودن مسیر جدید.**

## G-14 · `mypy --strict` روی `core/`
موتورهای عددی باید سخت‌ترین تایپ را داشته باشند.

## G-15 · gitleaks و CodeQL
هر دو رایگان. PINهای دمو در متن عمومی منتشر شده‌اند.

## G-16 · job جهش تاریخ
سوئیت با `TZ` مختلف + تاریخ‌های آینده (`libfaketime`).
**این پروژه با تقویم جلالی مستعد این باگ است** — یک بار قربانی شد.

---

# معیار خروج LOOP G
```
□ ۱۶ تسک DONE
□ ۱۲/۱۲ مسیر E2E سبز در CI
□ Lighthouse: perf ≥ ۹۰، a11y ≥ ۹۵
□ axe: ۰ نقض critical
□ k6: P95 در بودجه
□ mutmut: ≥ ۷۰٪ روی core/
□ ماتریس مجوز: ۴۹×۶ کامل
□ mypy --strict روی core/ → exit 0
□ gitleaks و CodeQL سبز
□ سوئیت در ۴ منطقهٔ زمانی سبز
```
**عدد:** `E2E: ۰ → ۱۲` · `LCP: نامعلوم → عدد` · `P95: نامعلوم → عدد` · `دروازه‌های CI: ۷ → ۹`
